-- =============================================================================
-- 20250125000000_create_todos.down.sql: マイグレーション（DOWN）
-- =============================================================================
-- このファイルはマイグレーションをロールバック（取り消し）するためのファイル。
-- sqlx migrate revert コマンドで実行される。
--
-- .down.sql は .up.sql の逆操作を行う:
-- - .up.sql で CREATE TABLE した場合 → .down.sql で DROP TABLE
-- - .up.sql で ADD COLUMN した場合 → .down.sql で DROP COLUMN
--
-- 注意: ロールバックするとデータが失われる可能性がある
-- =============================================================================

-- todos テーブルを削除（インデックスも自動的に削除される）
-- IF EXISTS: テーブルが存在しない場合でもエラーにならない
DROP TABLE IF EXISTS todos;

# =============================================================================
# spin-axum-todo: 環境変数テンプレート
# =============================================================================
# このファイルを .env にコピーして使用してください:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Core Layer (axum)
# -----------------------------------------------------------------------------

# サーバーのリッスンアドレス
APP_ADDR=0.0.0.0:3001

# PostgreSQL 接続文字列（Writer/Reader 分離）
DATABASE_WRITER_URL=postgres://app:app@localhost:5432/app
DATABASE_READER_URL=postgres://app:app@localhost:5432/app

# Redis 接続文字列
REDIS_URL=redis://localhost:6379

# Edge 層からのリクエスト検証用シークレット
# 本番環境では必ず変更してください
EDGE_SECRET=super-secret-edge-key

# -----------------------------------------------------------------------------
# Edge Layer (Spin)
# -----------------------------------------------------------------------------

# JWT 署名用シークレット
# 本番環境では必ず変更してください
# 注: Edge 層（auth コンポーネント）と Core 層で同じ値を使用する必要があります
JWT_SECRET=super-secret-key

# JWT トークンの有効期間（時間）
JWT_EXPIRY_HOURS=24

# Core 層の URL
CORE_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# S3/LocalStack
# -----------------------------------------------------------------------------

# S3 エンドポイント URL（LocalStack 用）
# 本番環境では未設定にすると AWS 標準エンドポイントを使用
S3_ENDPOINT_URL=http://localhost:4566

# S3 バケット名
S3_BUCKET=todo-files

# AWS リージョン
AWS_DEFAULT_REGION=ap-northeast-1

# AWS 認証情報（LocalStack 用のダミー値）
# 本番環境では IAM ロールまたは実際の認証情報を使用
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# -----------------------------------------------------------------------------
# ログ設定
# -----------------------------------------------------------------------------

# ログレベル（error, warn, info, debug, trace）
RUST_LOG=info
